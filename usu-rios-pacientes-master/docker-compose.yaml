version: '3.8'

services:
  users-db:
    image: postgres:16
    container_name: users-isaude-db-local
    restart: always
    environment:
      POSTGRES_USER: ${DB_USER:-postgres}
      POSTGRES_PASSWORD: ${DB_PASS:-postgres}
      POSTGRES_DB: ${DB_NAME:-users_isaude_db}
    ports:
      - "5435:5432" # Porta mapeada para evitar conflito com padrão
    volumes:
      - users_pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${DB_USER:-postgres}"]
      interval: 10s
      timeout: 5s
      retries: 5

  users-redis:
    image: redis:alpine
    container_name: users-isaude-redis-local
    restart: always
    ports:
      - "6379:6379"
    volumes:
      - users_redisdata:/data

  users-api:
    build: .
    container_name: users-isaude-back-end-local
    restart: unless-stopped
    env_file: .env
    environment:
      - PORT_API=80
      - DB_HOST=users-db
      - REDIS_HOST=users-redis
      - DB_PORT=5432 # Porta interna do container do banco
    ports:
      - "8081:80"
    volumes:
      - .:/src
      - /src/node_modules
      # Garante que o arquivo de credenciais do Firebase seja montado
      - ./serviceAccountKey.json:/src/serviceAccountKey.json
    depends_on:
      users-db:
        condition: service_healthy
      users-redis:
        condition: service_started
    command: npm run dev

volumes:
  users_pgdata:
  users_redisdata: